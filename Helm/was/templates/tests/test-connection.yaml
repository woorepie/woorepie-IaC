apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "was-app.fullname" . }}-test-connection"
  labels:
    {{- include "was-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "was-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
