apiVersion: v1
kind: Service
metadata:
  name: {{ include "admin-was.fullname" . }}
  labels:
    {{- include "admin-was.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "admin-was.selectorLabels" . | nindent 4 }}
